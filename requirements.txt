# Jetson Orin / JetPack 6.x / CUDA 12.6 / Python 3.10.
#
# PyQt5 is provided by Ubuntu on Jetson:
#   sudo apt install python3-pyqt5
# Create the venv with:
#   python3 -m venv --system-site-packages .venv

--index-url https://pypi.jetson-ai-lab.io/jp6/cu126
--extra-index-url https://pypi.org/simple

torch==2.8.0
torchvision==0.23.0

ultralytics==8.4.53

# NumPy must stay <2: the Jetson torch 2.8.0 wheel is built against NumPy 1.x.
# `yolo export` auto-installs the onnx toolchain and will drag in NumPy 2 unless
# these are pinned (onnx>=1.17 requires ml_dtypes>=0.5.4 which requires NumPy>=2).
numpy>=1.26,<2
onnx==1.16.2
ml-dtypes<0.5.0
opencv-python==4.10.0.84
Pillow>=10,<13
PyYAML>=6,<7

joblib==1.5.3
scikit-learn==1.6.1
scipy==1.15.3

matplotlib==3.10.8
pandas==2.2.3

pytest>=8,<10
